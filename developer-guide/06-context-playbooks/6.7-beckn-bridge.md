# 6.7 Beckn Bridge — playbook

How to implement the Beckn Bridge. The Bridge is **not a bounded context** — it's a specialized Interface-Layer adapter, the sole place Beckn protocol vocabulary is permitted. Every other context in the system is Beckn-naive by design ([§4 of CLAUDE.md](../../CLAUDE.md)).

> **Read first**: [handoff §3 Beckn integration](../../handoff/03-beckn-integration.md), [§2.7 of CLAUDE.md](../../CLAUDE.md) (Bridge position), [§4 of CLAUDE.md](../../CLAUDE.md) (full Bridge rules), [ADR-0001](../../decisions/0001-bpp-network-identity.md), [ADR-0017](../../decisions/0017-order-and-fulfillment.md), [ADR-0019](../../decisions/0019-manual-catalog-republication.md).

## At a glance

| Aspect | Detail |
|---|---|
| Phase landed | [Phase 6](../05-phases.md) — Beckn Bridge |
| Architectural position | Interface-Layer adapter, sibling of `contexts/` and `admin-api/` |
| Owns | The mapping registry + protocol mechanics. **No domain.** |
| Sole protocol-aware zone | Beckn vocabulary lives only here |
| External systems | Beckn registry; CDS; configured BAP callback URLs |
| Cross-context calls | Invokes use cases from every context (Order, Catalog, Inventory availability reads) |
| **Emits no domain events** | Bridge emits Beckn protocol messages, not domain events |
| First migration | `0008__bridge.sql` (protocol-boundary idempotency + outbound queue) |

## Folder structure

Per [§2.5 of `02-repo-layout.md`](../02-repo-layout.md):

```
bridge/
├── inbound/                                   handlers for messages BAP → BPP
│   ├── select.ts                              validates + maps + invokes Order.CreateQuote
│   ├── init.ts
│   ├── confirm.ts
│   ├── status.ts
│   └── cancel.ts
├── outbound/                                  callbacks BPP → BAP
│   ├── on-select.ts
│   ├── on-init.ts
│   ├── on-confirm.ts
│   ├── on-status.ts
│   └── on-cancel.ts
├── catalog-publisher/
│   ├── publish-catalog-on-event.ts            event subscriber: republishes to CDS
│   └── catalog-projection-builder.ts          composes the wire-shape from Catalog + Inventory
├── mapping-registry/                          explicit domain ↔ wire mappings per [§4.6 of handoff](../../handoff/03-beckn-integration.md)
│   ├── README.md                              the registry index — what maps to what, per version
│   ├── order-to-contract.ts
│   ├── line-item-to-commitment.ts
│   ├── product-to-resource.ts
│   ├── store-to-provider.ts
│   ├── catalog-projection.ts                  multi-catalog projection
│   ├── voucher-to-offer.ts
│   ├── fulfillment-to-performance.ts
│   ├── payment-status-to-settlement.ts
│   ├── locale.ts                              LocalizedText.get(requested_locale) → wire
│   ├── errors.ts                              domain error → Beckn error code
│   └── beckn-status-to-domain.ts              reverse mapping for inbound
├── protocol/
│   ├── signing.ts                             sign outbound; produce Authorization header
│   ├── verification.ts                        verify inbound signature against registry-fetched key
│   ├── envelope.ts                            construct + parse Beckn envelope
│   ├── context-block.ts                       build + extract `context` block (transaction_id, message_id, …)
│   ├── correlation.ts                         transaction_id / message_id tracking
│   ├── idempotency.ts                         protocol-boundary dedup by (transaction_id, message_id)
│   └── versions/
│       └── v2/                                v2-specific handlers / mappings
├── beckn-types/                                manually authored or generated from ion-specs
└── tests/
    ├── inbound/                                per-message-type with real ion-specs payloads
    ├── outbound/                               per-callback projection tests
    ├── mapping-registry/                       round-trip tests
    └── e2e/                                    full Beckn flow with stub BAP
```

## What's NOT in the Bridge

- **Domain entities.** The Bridge has none. It invokes use cases.
- **Business rules.** "An item is available if stock > 0" is a domain rule. The Bridge asks the Application Layer.
- **Direct persistence.** The Bridge writes only to its own protocol-boundary tables (idempotency, outbound queue). It reads through Application-Layer use cases.
- **Domain events.** The Bridge emits Beckn messages on the wire, not domain events.

## Network identity (per [ADR-0001](../../decisions/0001-bpp-network-identity.md))

The platform appears on the Beckn network as **a single BPP**. Operational config:

| Config | Source |
|---|---|
| `bpp-id` | Deploy-time env var |
| `bpp-uri` | Deploy-time env var (single platform-wide callback endpoint) |
| Signing key material | Secrets manager |
| Registry credentials | Secrets manager |
| Registry endpoint | Per-environment config |
| CDS endpoint(s) | Per-environment config |

Stores project as Beckn `provider` nodes within BPP responses; provider IDs are deterministically derived from internal store identifiers (mapping lives in `store-to-provider.ts`).

## Use cases (handlers + projections)

The Bridge has no domain use cases. Its "use cases" are inbound handlers (calls into the Application Layer) and outbound projections (calls out to the network).

### Inbound handlers

| Handler | BAP → BPP | Invokes Application use case |
|---|---|---|
| `handleSelect(payload)` | `/select` (Contract w/ items, optional voucher) | `Order.CreateQuote(store_id, items, voucher_code?, beckn_buyer_ref)` |
| `handleInit(payload)` | `/init` (Contract w/ consumer details) | `Order.Initiate(order_id, contact_snapshot)` |
| `handleConfirm(payload)` | `/confirm` (Contract) | `Order.Confirm(order_id)` |
| `handleStatus(payload)` | `/status` | `Order.GetStatus(order_id)` |
| `handleCancel(payload)` | `/cancel` (with reason) | `Order.Cancel(order_id, reason, by_actor = bridge_principal)` |

Each handler does, in order:

1. **Verify signature** (`protocol/verification.ts`) against the registry-fetched BAP key.
2. **Validate payload** against the ion-specs schema for the declared protocol version.
3. **Idempotency check** at the protocol boundary by `(transaction_id, message_id)`. Duplicate → return cached ack, no domain call.
4. **Extract business intent** via `mapping-registry/` (e.g., `Contract → CreateQuoteInput`).
5. **Invoke the Application use case** with domain-shaped inputs.
6. **Return immediate acknowledgement** (Beckn is async — substantive response is delivered via the matching `/on_*` callback).
7. **Schedule outbound projection** (enqueue the corresponding `on-*` callback for asynchronous dispatch).

### Outbound projections (callbacks)

| Projection | BPP → BAP | Built from |
|---|---|---|
| `projectOnSelect(order)` | `/on_select` | Order's Quote |
| `projectOnInit(order)` | `/on_init` | Order's populated Contract (with `contact_snapshot`) |
| `projectOnConfirm(order)` | `/on_confirm` | Confirmed Order |
| `projectOnStatus(order)` | `/on_status` | Current Order state |
| `projectOnCancel(order)` | `/on_cancel` | Cancelled Order |

Each projection does:

1. **Build wire payload** via `mapping-registry/` (e.g., `Order → Contract`).
2. **Enrich with protocol metadata** (context block, signatures).
3. **Sign** (`protocol/signing.ts`).
4. **Dispatch to BAP callback URL** with retry on transport failure.

### CDS publishing (event-driven)

The catalog publisher subscribes to domain events that affect what BAPs should see:

| Subscribed event | Behavior |
|---|---|
| `tenancy.store_status_changed` | Re-project store's catalogs; publish to CDS |
| `tenancy.store_republish_requested` | Manual republish for one store |
| `tenancy.org_republish_all_requested` | Fan out: republish all stores in the Org |
| `catalog.product_created`, `_updated`, `_published`, `_archived`, `_restored`, `_media_updated` | Re-project affected store's catalogs |
| `catalog.product_variant_*` | Same |
| `catalog.product_category_assigned`, `_unassigned` | Same |
| `catalog.store_catalog_created`, `_renamed`, `_deleted`, `product_included_in_catalog`, `_removed_from_catalog` | Re-project store's catalogs |

The publisher is **idempotent** (re-projection from current state is always safe).

## Ports declared

The Bridge declares **external** ports for protocol-side dependencies:

| Port | Purpose | Implementation |
|---|---|---|
| `BecknNetworkClientPort` | Outbound callback dispatch | `platform-infra/beckn-network/` adapter |
| `BecknRegistryClientPort` | Fetch BAP keys, verify participant identity | `platform-infra/beckn-network/` adapter |
| `BecknSigningKeyPort` | Access platform's signing key | `platform-infra/beckn-network/` adapter |
| `CDSClientPort` | Publish catalog updates to CDS | `platform-infra/beckn-network/` adapter |
| `IonSpecsValidatorPort` | Validate payloads against ion-specs schemas per version | `platform-infra/beckn-network/` adapter |

**Provides** to other code: nothing. The Bridge is consumed only by HTTP requests from the network.

The Bridge **calls** every context's Application-Layer use cases as needed — no direct repository access ever.

## Migration sketch

`migrations/0008__bridge.sql`:

```sql
-- Protocol-boundary idempotency: dedup inbound by (transaction_id, message_id)
CREATE TABLE bridge_inbox (
  transaction_id text NOT NULL,
  message_id text NOT NULL,
  protocol_version text NOT NULL,
  message_type text NOT NULL,                   -- 'select' | 'init' | 'confirm' | ...
  received_at timestamptz NOT NULL DEFAULT now(),
  ack_response jsonb NOT NULL,                  -- cached acknowledgement returned to BAP
  PRIMARY KEY (transaction_id, message_id)
);

-- Outbound queue: callbacks awaiting dispatch / retry
CREATE TABLE bridge_outbox (
  id text PRIMARY KEY,
  transaction_id text NOT NULL,                 -- correlation
  message_id text NOT NULL,                     -- new message_id for this callback
  bap_id text NOT NULL,
  bap_callback_url text NOT NULL,
  payload jsonb NOT NULL,
  state text NOT NULL DEFAULT 'pending',        -- pending | sent | failed | dead
  attempt_count int NOT NULL DEFAULT 0,
  last_attempt_at timestamptz,
  last_error text,
  created_at timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX idx_bridge_outbox_pending
  ON bridge_outbox (created_at) WHERE state = 'pending';
```

## Cross-context integration

| Direction | Detail |
|---|---|
| **Calls** | All contexts' Application-Layer use cases (Order primarily; Catalog read-side; Inventory `BulkGetAvailability`) |
| **Imports forbidden** | Any context's `domain/` or `infrastructure/` |
| **Subscribes to** | `tenancy.store_status_changed`, `tenancy.*_republish_*`, all `catalog.*` mutation events |
| **Events emitted** | None (the Bridge emits Beckn protocol messages, not domain events) |
| **Consumed by** | Only the network. No internal consumers. |

## Implementation order (within the Bridge)

1. **Protocol scaffolding** — `signing`, `verification`, `envelope`, `context-block`. Wire up `BecknSigningKeyPort` and `BecknRegistryClientPort` adapters.
2. **`bridge_inbox` table** + `protocol/idempotency.ts` — dedup by `(transaction_id, message_id)`.
3. **`bridge_outbox` table** + dispatcher for outbound callbacks (with retry).
4. **`mapping-registry/` skeleton** + `README.md` indexing entries.
5. **Inbound `/select`** — verify, validate, dedup, map → `Order.CreateQuote`, ack.
6. **Outbound `/on_select`** — project Order's Quote → wire Contract → sign → dispatch.
7. **Correlation infrastructure** (`protocol/correlation.ts`) — match outbound callbacks to inbound transactions.
8. **Inbound `/init`** + **outbound `/on_init`**.
9. **Inbound `/confirm`** + **outbound `/on_confirm`**.
10. **Inbound `/status`** + **outbound `/on_status`**.
11. **Inbound `/cancel`** + **outbound `/on_cancel`**.
12. **Catalog publisher** — subscribe to events, project, publish to CDS. Wire-state mapping (Active → `isActive: true`; Paused / Suspended → `isActive: false`; Draft → not published).
13. **Multi-catalog projection** (`catalog-projection.ts` mapping entry) — Default + additional Catalogs per [§3.4.1 of handoff](../../handoff/03-beckn-integration.md).
14. **Locale resolution** (`mapping-registry/locale.ts`) — per-request `context.language` resolved via `LocalizedText.get`. No automatic translation.
15. **Error mapping** (`mapping-registry/errors.ts`) — every domain error type to a Beckn error code; unmappable → generic protocol error AND log mismatch.
16. **Manual republication handler** — `tenancy.store_republish_requested` and `org_republish_all_requested` subscribers.

## Test checklist (heaviest in the codebase)

Per [§7.7 of `07-testing.md`](../07-testing.md). All tests use **real ion-specs payloads**.

**Inbound parsing** (per message type × per supported protocol version):
- [ ] Valid payload → invokes the expected Application use case with expected inputs.
- [ ] Invalid signature → rejected with appropriate Beckn error.
- [ ] Schema-invalid payload → rejected.
- [ ] Duplicate by `(transaction_id, message_id)` → short-circuits; cached ack returned; Application use case invoked exactly once.

**Outbound projection** (per callback type):
- [ ] Domain object → wire payload round-trips through ion-specs validation.
- [ ] Required protocol metadata present (context, signatures).
- [ ] Locale resolution: BAP-requested locale present → returned; absent → default-locale fallback.

**Mapping registry** (round-trip per entry):
- [ ] `order-to-contract`: Confirmed Order → wire Contract with `status: ACTIVE`; Cancelled → `CANCELLED`; etc.
- [ ] `voucher-to-offer`: Voucher → wire Offer; round-trip.
- [ ] `errors`: every `TypedError` subclass in the domain has a Beckn error mapping (or falls through to generic with logged mismatch).
- [ ] `locale`: `LocalizedText` with multiple entries → correct entry returned per request locale.

**Correlation**:
- [ ] Outbound `/on_select` carries the matching `transaction_id` from the inbound `/select`.
- [ ] Cross-flow correlation: a `/cancel` for an order created earlier in the same transaction finds the right Order.

**CDS publishing**:
- [ ] `tenancy.store_status_changed` to Active triggers a publish with `isActive: true`.
- [ ] `tenancy.store_status_changed` to Paused triggers a publish with `isActive: false`.
- [ ] `catalog.product_published` triggers a re-projection.
- [ ] Manual `store_republish_requested` triggers a re-projection.
- [ ] `org_republish_all_requested` fans out per store in the Org.
- [ ] Re-projection is idempotent (publishing the same state twice is safe).

**End-to-end** (stub BAP — per [§7.8 of `07-testing.md`](../07-testing.md)):
- [ ] `/select → /on_select → /init → /on_init → /confirm → /on_confirm` happy path.
- [ ] `/cancel` from Initiated → reservations released.
- [ ] `/cancel` from Confirmed → voucher reverted; payment Refunded.
- [ ] Quote TTL expiry → `/on_status` reflects Expired.

## Gotchas

- **Beckn vocabulary stays in the Bridge.** Never in domain, never in Application, never in admin-api. The lint rule (per [§2.8 of `02-repo-layout.md`](../02-repo-layout.md)) enforces this — but watch in code review too. If you see `descriptor`, `provider`, `resource`, `offer`, `contract`, `commitment`, `consideration`, `performance`, `context`, `intent` anywhere outside `bridge/`, that's leakage.
- **Beckn is async, callback-driven.** Acknowledge inbound requests immediately; deliver substantive responses via callbacks. Correlation by `transaction_id` is the Bridge's problem, not the Application Layer's.
- **The Bridge is the only place protocol-level idempotency lives.** Domain `Idempotency-Key` middleware doesn't replace it — Beckn duplicates come from BAP retries, not from clients calling our HTTP. Use `(transaction_id, message_id)` as the dedup key.
- **Errors translate in both directions.** Domain errors → Beckn error codes (via `mapping-registry/errors.ts`); Beckn protocol errors are handled inside the Bridge and never surface as domain errors to inner layers.
- **Unmappable domain errors → generic protocol error AND log the mismatch.** Silent loss of information is forbidden ([§4.5 of handoff](../../handoff/03-beckn-integration.md)).
- **Multi-catalog projection has Bridge-only mapping** ([§3.4.1 of handoff](../../handoff/03-beckn-integration.md)). The domain has `Catalog` entities; the wire has whatever shape the registry decides. Don't push that decision into Catalog.
- **CDS is mandated**; BPP does NOT field `/discover` directly ([ADR-0017](../../decisions/0017-order-and-fulfillment.md)). Discovery is BAP → CDS → BPP transactional only.
- **Registry lifecycle is operational**, not in-band. The Bridge consumes registry credentials but doesn't register/unregister itself. Key rotation is a runbook.
- **Wire-state mapping lives in `mapping-registry/`**, not in the domain:
  - Store status `Active` → Catalog `isActive: true`.
  - Store status `Paused` / `Suspended` → Catalog `isActive: false`.
  - Store status `Draft` → not published.
  - Order status `Created` / `Initiated` → Contract `DRAFT`.
  - Order status `Confirmed` → Contract `ACTIVE`.
  - Order status `Fulfilled` → Contract `COMPLETE`.
  - Order status `Cancelled` / `Expired` → Contract `CANCELLED`.
- **Versioning lives in `protocol/versions/`.** Per-version handlers + mappings; multiple versions can coexist. Version is detected from inbound `context.core_version` (or equivalent). A Beckn version migration is a Bridge-only change.
- **Outbound dispatch can fail.** The `bridge_outbox` table + dispatcher handles retry. Failed callbacks after N attempts go to `state: 'dead'` for operator review.
- **The Bridge does NOT emit domain events.** Anyone wanting "the Bridge sent a callback" as an audit signal should check `bridge_outbox` or rely on the domain event upstream (e.g., `order.confirmed`) that triggered the callback.
- **Signature verification happens before payload parsing.** Don't trust the payload until the signature is validated against the registry-fetched key.

## References

- [handoff §3 Beckn integration](../../handoff/03-beckn-integration.md) — full Bridge architecture
- [§2.7 of CLAUDE.md](../../CLAUDE.md) — Bridge position
- [§4 of CLAUDE.md](../../CLAUDE.md) — full Bridge rules + responsibilities
- [ADR-0001](../../decisions/0001-bpp-network-identity.md) — Network identity (single BPP)
- [ADR-0017](../../decisions/0017-order-and-fulfillment.md) — CDS-mediated discovery + v2 vocabulary
- [ADR-0019](../../decisions/0019-manual-catalog-republication.md) — manual republication
- [ion-specs](file:///Users/danielignatius/mydev/personal/ion-specs) — protocol source of truth (local reference)
- [`02-repo-layout.md`](../02-repo-layout.md) — Bridge folder structure
- [`07-testing.md`](../07-testing.md) — Bridge testing posture
